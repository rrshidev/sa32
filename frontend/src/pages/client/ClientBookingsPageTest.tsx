import { Box, Typography } from '@mui/material';

const ClientBookingsPageTest = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Мои записи - Тестовая страница</Typography>
      <Typography>Если вы видите этот текст, то роут работает!</Typography>
    </Box>
  );
};

export default ClientBookingsPageTest;
